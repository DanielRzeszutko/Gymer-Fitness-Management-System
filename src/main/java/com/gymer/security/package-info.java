/**
 * Package responsible for all security features.
 * Contains one class with configuration and some smaller components connected in more or less with security functionality.
 * Components are responsible for: login, registration, logout, obtaining active users session, validation if
 * selected user is logged in and manipulate only self resources, verification functionality responsible for
 * two stage verification when register new account and some kind of smaller features, etc. changing user password.
 *
 * @author everybody
 */
package com.gymer.security;
